<h1>Post list</h1>

<%= link_to 'Create new post', new_post_path %>

<% @posts.each do |post| %>
  <div>
    <h2><%= link_to post.title, post %></h2>
    <p><%= post.created_at.strftime("%d/%m/%y %H:%M:%S") %></p>
  </div>
<% end %>
